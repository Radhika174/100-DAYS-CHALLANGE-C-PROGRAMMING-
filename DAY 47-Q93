/*Q93: Check if two strings are anagrams of each other.

Sample Test Cases:
Input 1:
listen
silent
Output 1:
Anagrams

Input 2:
hello
world
Output 2:
Not anagrams

*/
#include <stdio.h>
#include <string.h>
int main()
{
    char strg1[100]; 
    char strg2[100];
    printf("Enter first string:");
    scanf("%s",strg1);
    printf("Enter second string:");
    scanf("%s",strg2);
    char ch;
    char ch2;
    int len1=strlen(strg1);
    int len2=strlen(strg2);
    int p=0;
    if(len1==len2)
    {
    for(int i=0;i<strlen(strg1);i++)
    {
        ch = strg1[i];
        for(int j=0;j<len2;j++)
        {
            ch2=strg2[i];
            if(ch==ch2)
            p++;
        }
       
    }
    }
    if(p==len1)
    printf("Anagrams");
    else
    printf("Not Anagrams");
    return 0;
}
